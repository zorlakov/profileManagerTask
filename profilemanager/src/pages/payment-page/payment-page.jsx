import React, { Component } from 'react';
import ProfileManager from '../../components/profile-manager/profile-manager';

export default class PaymnetPage extends Component {
  render() {
    return <ProfileManager className='page' />;
  }
}
